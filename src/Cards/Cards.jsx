
const Cards = ({cards}) => {
    console.log(cards);
    return (
        <div>
            This is card section
            {
                // cards.map(card=> )
            }
        </div>
    );
};

export default Cards;